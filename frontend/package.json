{
  "name": "screen-overlay-app",
  "version": "1.0.0",
  "description": "A screen overlay application built with Electron.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "rebuild": "electron-rebuild -f -w robotjs",
    "postinstall": "electron-rebuild -f -w robotjs"
  },
  "devDependencies": {
    "electron": "^31.0.1",
    "electron-rebuild": "^3.2.9"
  },
  "dependencies": {
    "electron-log": "^5.4.2",
    "node-fetch": "^3.3.2",
    "robotjs": "^0.6.0"
  }
}
